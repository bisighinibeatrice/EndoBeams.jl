# The matrix assembly functions should be put here



"""
    assemble!(globalmatrix, beam::Beam)
	
Assemble a global stiffness matrix from a beam element.
"""
function assemble!(globalmatrix, beam::Beam)

end